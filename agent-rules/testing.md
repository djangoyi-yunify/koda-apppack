## 测试脚本编写与运行规则

### 粒度划分
- 将测试拆分为独立的、可单独运行的最小任务，每个任务只验证一个具体行为或假设
- 禁止在一个测试运行中串联多个不相关任务

### 执行顺序
1. 逐个任务进行测试
2. 单个任务通过后，再进入下一个任务
3. 所有单个任务均通过后，最后执行完整的回归测试

### 问题处理
- 测试单个任务时，一旦发现问题，立即停止并解决
- 禁止等到所有任务测试完毕后再统一处理问题
- 修复后需重新运行当前任务，确认通过后再继续

### 回归测试
- 单个任务全部通过后，必须运行完整回归测试
- 回归测试失败时，按上述“问题处理”流程执行
